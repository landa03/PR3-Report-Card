using System;

namespace PR3_Report_Card
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            usar:
            estacion de camiones, donde un camion es un estudiante (objeto tipo est.) con nombre y una lista de materias con califas

            */
            // Menu menu = new Menu();
            // menu.Display();

            // Queue queue = new Queue();

            // System.Console.WriteLine(queue);

            // System.Console.WriteLine($"queue.Front() => {queue.Front()}");
            //Estudents Bol = new Estudents();
            //Bol.AddStudent();
            //Bol.AddMateria();
            Menu yea = new Menu();
            yea.Display();
        }
    }
}
//todo no subestimar un proyecto que suena sencillo
//todo tratar con problemas lo antes posible aunque no sea para terminarlos, solo para entenderlos